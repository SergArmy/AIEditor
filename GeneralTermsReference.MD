# Inline-редактор документации для 1С

## Содержание
- [Быстрый старт](#быстрый-старт)
- [Архитектура](#архитектура)
- [Разработка](#разработка)
- [Спецификации блоков](#спецификации-блоков)
- [План разработки](#план-разработки)
- [Прогресс](#прогресс)
- [TODO](#todo)
- [Команды для AI](#команды-для-ai)
- [Примечания для разработчиков](#примечания-для-разработчиков)

## Быстрый старт

**Начни здесь:**

```bash
# 1. Создай базовую структуру проекта
mkdir editor-1c && cd editor-1c
mkdir -p src/{elements/{base,structural,content,specialized},stores,services,utils}

# 2. Начни с базового класса блока
# Файл: src/elements/base/base-block.js
```

**Технологический стек**

- Core Stack:
  - Web Components (Custom Elements)
  - Monaco Editor для подсветки кода
  - Lit Element для реактивности
  - Font Awesome Light для иконок
  - CSS Variables + Tailwind для стилей
- State Management:
  - Zustand для состояния
  - Immer для иммутабельных обновлений

**Первые 5 файлов**

1. `src/elements/base/base-block.js` — базовый класс всех блоков.
2. `src/elements/structural/document-block.js` — корневой контейнер.
3. `src/elements/content/text-block.js` — простой текстовый блок.
4. `examples/minimal.html` — минимальный пример.
5. `src/stores/document-store.js` — управление состоянием.

## Архитектура

**Структура проекта**

```text
editor-1c/
├── src/
│   ├── elements/                 # Custom Elements
│   │   ├── base/                 # Базовые классы (start here)
│   │   │   ├── base-block.js
│   │   │   ├── editable-block.js
│   │   │   └── container-block.js
│   │   ├── structural/           # Структурные блоки
│   │   │   ├── document-block.js
│   │   │   ├── section-block.js
│   │   │   ├── header-block.js
│   │   │   └── toc-block.js
│   │   ├── content/              # Контентные блоки
│   │   │   ├── text-block.js
│   │   │   ├── code-block.js
│   │   │   ├── image-block.js
│   │   │   ├── table-block.js
│   │   │   └── quote-block.js
│   │   └── specialized/          # Специализированные блоки
│   │       ├── comparison-block.js
│   │       ├── diagram-block.js
│   │       ├── checklist-block.js
│   │       ├── action-block.js
│   │       ├── steps-block.js
│   │       ├── definition-block.js
│   │       └── roles-block.js
│   ├── components/               # Вспомогательные компоненты
│   │   ├── context-menu/
│   │   ├── icon-picker/
│   │   ├── role-manager/
│   │   └── toolbar/
│   ├── stores/                   # State management
│   │   ├── document-store.js
│   │   ├── role-store.js
│   │   ├── icon-store.js
│   │   └── command-store.js
│   ├── services/                 # Сервисы
│   │   ├── monaco-service.js
│   │   ├── export-service.js
│   │   └── template-service.js
│   └── utils/                    # Утилиты
│       ├── selection-utils.js
│       ├── keyboard-utils.js
│       └── dom-utils.js
├── examples/                     # Примеры использования
│   ├── minimal.html
│   ├── blocks-showcase.html
│   └── advanced-features.html
├── docs/                         # Документация
│   ├── ARCHITECTURE.md
│   ├── BLOCK_SPECIFICATION.md
│   ├── API_REFERENCE.md
│   └── DEVELOPMENT_GUIDE.md
└── styles/                       # Стили
    ├── main.css
    ├── variables.css
    └── blocks/
```

**Ключевые принципы**

- Custom Elements — каждый блок это независимый веб-компонент.
- Единый реестр ролей — роли хранятся централизованно и переиспользуются.
- Контекстное меню — интеллектуальные команды по контексту.
- Monaco Editor — профессиональная подсветка кода для 1С.
- Система плагинов — расширяемость через плагины.

## Разработка

**Шаблон для нового блока**

```javascript
// CURSOR_TEMPLATE: Новый блок
// 1. Замени MyNewBlock на имя блока
// 2. Выбери правильный родительский класс
// 3. Заполни TODO комментарии

/**
 * @element my-new-block
 * @description [ОПИСАНИЕ_БЛОКА]
 *
 * @attributes
 * @attr {string} block-id - Уникальный идентификатор
 * @attr {boolean} editable - Режим редактирования
 *
 * @methods
 * @method serialize() => object - Сериализация данных
 * @method deserialize(data) - Восстановление из данных
 */
class MyNewBlock extends BaseBlock {
  static get observedAttributes() {
    return ['block-id', 'editable', /* TODO: Добавь атрибуты */];
  }

  constructor() {
    super();
    // TODO: Инициализация состояния
  }

  connectedCallback() {
    super.connectedCallback();
    // TODO: Настройка DOM
  }

  // TODO: Добавь специфичные методы
}

customElements.define('my-new-block', MyNewBlock);
```

**Правила разработки**

- Наследование — все блоки наследуются от `BaseBlock` или `EditableBlock`.
- JSDoc — документируй все публичные методы.
- Атрибуты — используй `observedAttributes` для реактивности.
- События — применяй Custom Events для коммуникации между блоками.
- Регистрация — регистрируй элемент в конце файла.

**Тестирование**

```bash
# Всегда тестируй изменения в examples/minimal.html
# Добавляй блок в examples/blocks-showcase.html
```

## Спецификации блоков

### Базовые классы

**BaseBlock** (`src/elements/base/base-block.js`)

```javascript
/**
 * CURSOR_CONTEXT: Базовый класс для всех блоков
 * CURSOR_ACTION: При создании нового блока наследуй от этого класса
 */
class BaseBlock extends HTMLElement {
  static get observedAttributes() {
    return ['block-id', 'selected', 'editable', 'collapsed'];
  }

  constructor() {
    super();
    this.blockId = this.generateId();
    this.attachShadow({ mode: 'open' });
  }

  // CURSOR_EXAMPLE: Генерация ID блока
  generateId() {
    return `block_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
  }

  // CURSOR_EXAMPLE: Сериализация блока
  serialize() {
    return {
      id: this.blockId,
      type: this.tagName.toLowerCase(),
      content: this.getContent(),
      attributes: this.getAttributes()
    };
  }

  // TODO: Добавь методы getContent(), getAttributes()
}
```

**EditableBlock** (`src/elements/base/editable-block.js`)

```javascript
/**
 * CURSOR_CONTEXT: Блоки с редактируемым содержимым
 * CURSOR_ACTION: Используй для текстовых блоков, кода и т.д.
 */
class EditableBlock extends BaseBlock {
  setEditable(editable) {
    this.editable = editable;
    this.updateContentEditable();
  }

  getContent() {
    // TODO: Реализуй получение содержимого
  }

  setContent(content) {
    // TODO: Реализуй установку содержимого
  }
}
```

### Контентные блоки

**TextBlock** (`src/elements/content/text-block.js`)

```javascript
/**
 * @element text-block
 * @description Текстовый блок с поддержкой форматирования
 *
 * @attributes
 * @attr {string} variant - Вариант (paragraph, note, important)
 * @attr {string} alignment - Выравнивание (left, center, right)
 */
class TextBlock extends EditableBlock {
  static get template() {
    return `
      <style>
        .text-block {
          padding: var(--spacing-md);
          line-height: 1.7;
        }
        .text-block[contenteditable="true"] {
          outline: 2px solid var(--accent-color);
        }
      </style>
      <div class="text-block" contenteditable="true">
        <slot></slot>
      </div>
    `;
  }
}
```

**CodeBlock** (`src/elements/content/code-block.js`)

```javascript
/**
 * @element code-block
 * @description Блок кода с Monaco Editor
 *
 * @attributes
 * @attr {string} language - Язык программирования (1c, javascript, sql)
 * @attr {boolean} line-numbers - Показывать номера строк
 */
class CodeBlock extends BaseBlock {
  constructor() {
    super();
    this.language = '1c';
    this.monacoEditor = null;
  }

  async connectedCallback() {
    await this.initializeMonaco();
  }

  async initializeMonaco() {
    this.monacoEditor = await MonacoService.createEditor(
      this.shadowRoot.getElementById('editor-container'),
      this.language
    );
  }
}
```

### Специализированные блоки

**RolesBlock** (`src/elements/specialized/roles-block.js`)

```javascript
/**
 * @element roles-block
 * @description Блок для отображения ролей и прав доступа
 * CURSOR_NOTE: Интегрируется с единым реестром ролей
 */
class RolesBlock extends BaseBlock {
  constructor() {
    super();
    this.roleStore = RoleStore.getInstance();
  }

  renderRole(roleId) {
    const role = this.roleStore.getRole(roleId);
    return `
      <div class="role-item" style="--role-color: ${role.color}">
        <span class="role-name">${role.name}</span>
        <span class="role-description">${role.description}</span>
      </div>
    `;
  }
}
```

## План разработки

### Этап 1. Базовая инфраструктура (Неделя 1)
- [x] Создать структуру проекта.
- [x] Настроить базовую сборку.
- [ ] Реализовать `BaseBlock` и `EditableBlock`.
- [ ] Создать `DocumentStore` для управления состоянием.
- [ ] Реализовать `TextBlock` и `SectionBlock`.
- [ ] Создать минимальный пример (`examples/minimal.html`).

### Этап 2. Система редактирования (Неделя 2)
- [ ] Интегрировать Monaco Editor.
- [ ] Реализовать `CodeBlock` с подсветкой 1С.
- [ ] Создать блоки: `ImageBlock`, `TableBlock`, `QuoteBlock`.
- [ ] Реализовать базовое контекстное меню.
- [ ] Добавить систему Undo/Redo.

### Этап 3. Взаимодействия (Неделя 3)
- [ ] Реализовать Drag & Drop блоков.
- [ ] Добавить множественное выделение.
- [ ] Создать интеллектуальное контекстное меню.
- [ ] Реализовать систему ролей (`RoleStore`).
- [ ] Добавить меню выбора иконок.

### Этап 4. Специализированные блоки (Неделя 4)
- [ ] Реализовать `ComparisonBlock`, `DiagramBlock`.
- [ ] Создать `ChecklistBlock`, `ActionBlock`, `StepsBlock`.
- [ ] Добавить `DefinitionBlock`, `RolesBlock`.
- [ ] Интегрировать PlantUML для диаграмм.
- [ ] Создать блок оглавления (TOC).

### Этап 5. Интеграция 1С (Неделя 5)
- [ ] Расширить подсветку 1С в Monaco.
- [ ] Добавить ссылки на метаданные 1С.
- [ ] Создать шаблоны документов.
- [ ] Реализовать экспорт в HTML/PDF.
- [ ] Добавить импорт документов.

### Этап 6. Полировка (Неделя 6)
- [ ] Оптимизировать производительность.
- [ ] Добавить виртуализацию для больших документов.
- [ ] Реализовать тёмную тему.
- [ ] Протестировать accessibility (WCAG).
- [ ] Создать документацию.

## Прогресс

- Текущий статус: 🟡 В разработке.

**Завершённые задачи**

- [x] 2024-01-15: Создана структура проекта.
- [x] 2024-01-15: Написано ТЗ для Cursor AI.
- [x] 2024-01-15: Созданы основные директории.

**Текущие задачи**

- [ ] Реализация `BaseBlock` (в процессе).
- [ ] Создание `DocumentStore`.
- [ ] Разработка `TextBlock`.

**Следующие шаги**

1. Завершить `BaseBlock` и `EditableBlock`.
2. Создать `DocumentStore` с Zustand.
3. Реализовать `TextBlock` и протестировать в `examples/minimal.html`.

## TODO

**Высокий приоритет**

- [ ] Создать `src/elements/base/base-block.js`.
- [ ] Создать `src/elements/base/editable-block.js`.
- [ ] Создать `src/stores/document-store.js`.
- [ ] Реализовать `src/elements/content/text-block.js`.
- [ ] Создать `examples/minimal.html` для тестирования.

**Средний приоритет**

- [ ] Настроить `MonacoService`.
- [ ] Создать систему ролей (`RoleStore`).
- [ ] Реализовать контекстное меню.
- [ ] Добавить базовые стили из шаблона.

**Низкий приоритет**

- [ ] Создать блоки для диаграмм.
- [ ] Реализовать экспорт в PDF.
- [ ] Добавить систему плагинов.
- [ ] Создать продвинутые примеры.

## Команды для AI

**Быстрые запросы**

- «Создай новый блок [тип]» — используй шаблон из раздела «Разработка», наследуйся от правильного класса, добавь пример в `examples/blocks-showcase.html`.
- «Исправь баг в [имя-блока]» — проверь `BLOCK_SPECIFICATION.md`, протестируй в `examples/minimal.html`, обнови документацию.
- «Добавь новую функциональность в [компонент]» — следуй `DEVELOPMENT_GUIDE.md`, сохраняй обратную совместимость, добавь пример использования.
- «Оптимизируй производительность [компонента]» — протестируй рендеринг в больших документах, используй виртуализацию при необходимости, проверь на 1000+ блоков.

**Контекстные подсказки**

```javascript
// CURSOR_CONTEXT: Этот метод используется для сериализации блока
// CURSOR_ACTION: При реализации в дочерних классах вызывай super.serialize()
// CURSOR_NOTE: Все блоки должны поддерживать сериализацию/десериализацию

// CURSOR_EXAMPLE: Пример использования
const blockData = myBlock.serialize();
localStorage.setItem('block', JSON.stringify(blockData));
```

## Примечания для разработчиков

**Важные соглашения**

- Именование: используй kebab-case для имён элементов (`text-block`, `code-block`).
- Стили: сохраняй CSS переменные из оригинального шаблона.
- События: применяй префикс `editor-` для кастомных событий.
- Документация: обновляй `BLOCK_SPECIFICATION.md` при изменении API.

**Тестирование**

- Всегда тестируй в `examples/minimal.html`.
- Проверяй совместимость с существующими блоками.
- Тестируй на больших документах (100+ блоков).

**Производительность**

- Используй виртуализацию для документов > 200 блоков.
- Оптимизируй перерисовки с помощью Lit Element.
- Кэшируй тяжёлые операции (например, рендеринг диаграмм).

---

_Документ обновляется по мере разработки. Последнее обновление: ${current_date}_.